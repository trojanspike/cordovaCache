cordova plugin add add cordova-plugin-file

document.addEventListener("deviceready" , function(){
alert();
	/**********/
	describe(" Cordova Cache epecs " , function(){
	var Cache, Crypt;

	beforeAll(function(){
		console.log('@1');
		/* SI.cordovaCache('spec.test.cache' , function(cache, crypt){
			Cache = cache;
			Crypt = crypt;
		}); 
		*/

	});


		it(" Should be objects " , function(){
			console.log('@2');
			expect( typeof window.SI ).toBe("object");
			expect( typeof CryptoJS.AES ).toBe("object");
			expect( typeof window.requestFileSystem ).toBe("function");
			// expect( typeof Crypt ).toBe("object");
		});

		it(" Should be an object 2" , function(){
			console.log('@3');
			expect( typeof window.SI ).toBe("object");
			expect( typeof CryptoJS ).toBe("object");
			// expect( typeof Cache ).toBe("object");
			// expect( typeof Crypt ).toBe("object");
		});
	
	});
	/**********/
	});	
